{
  // 继承根目录的基础配置
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // ==================== 编译输出配置 ====================
    // 编译后的文件输出到 dist 目录
    "outDir": "dist",
    // 源代码的根目录（只编译 src 下的文件）
    "rootDir": "src",
    // 启用项目引用（允许其他包引用这个包）
    "composite": true,
    // ==================== 库支持 ====================
    // 包含 DOM 类型定义（如果需要使用 DOM API）
    // 包含 ES2023 标准库
    "lib": [
      "DOM",
      "DOM.Iterable",
      "ES2023"
    ],
    // 全局类型定义
    "types": [
      "node",
      "vitest/globals"
    ]
  },
  // ==================== 包含的文件 ====================
  // 编译 src 目录下的所有 TypeScript 文件
  "include": [
    "src/**/*.ts",
    "src/**/*.json"
  ],
  // ==================== 排除的文件 ====================
  // 不编译这些目录
  "exclude": [
    "node_modules", // 依赖包
    "dist", // 编译输出目录
    "**/*.test.ts" // 测试文件
  ]
}